<    @props(['title','type','name'])
    <div class="my-5">
        <input placeholder="{{$title}}" type="{{$type}}" name= {{$name}} class="bg-primaryGreen border border-primaryGreen py-1 px-2">
    </div>

